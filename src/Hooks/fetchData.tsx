// type fetchFun = {
//   url: string;
// };

// const fetchData = async <T>({ url }: fetchFun): Promise<T> => {
//   const res = await fetch(url);
//   if (!res.ok) {
//     throw new Error("Faild to fetch data.");
//   }
//   return await (res.json()) as T;
// };

// export default fetchData;
